---
import LinkInline from "./LinkInline.astro"
import ArrowRight from "./icons/ArrowRight.astro"

interface Props {
  title: string
  company: string
  description: string
  link?: string
  date: string
  current?: boolean
}

const { title, company, description, link, date, current = false } = Astro.props
---

<div
  class="relative mx-12 pb-12 grid before:absolute before:left-[-35px] before:block before:h-full before:border-l-2 before:border-gray-300 dark:before:border-white/20 before:content-[''] md:grid-cols-5 md:gap-10 md:space-x-4]"
>
  <div class="relative pb-12 md:col-span-2">
    <div class="sticky top-0">
      <!-- Title row with dot: flex ensures perfect vertical centering -->
      <div class="flex items-center">
        <span
          class="absolute -left-[42px] flex size-[14px] items-center justify-center"
          aria-hidden="true"
        >
          {current && (
            <span class="animate-ping motion-reduce:animate-none absolute inline-flex size-full rounded-full bg-[#3d0c0c]/30 dark:bg-[#c9a84c]/40"></span>
          )}
          <span
            class={`relative inline-flex size-[10px] rounded-full bg-[#3d0c0c] dark:bg-[#c9a84c] ${current ? "shadow-[0_0_8px_2px_rgba(61,12,12,0.3)] dark:shadow-[0_0_8px_2px_rgba(201,168,76,0.45)]" : ""}`}
          ></span>
        </span>
        <h3 class="text-xl font-bold text-[#3d0c0c] dark:text-[#dfc074]">{title}</h3>
      </div>
      <h4 class="font-semibold text-xl text-gray-800 dark:text-gray-200">{company}</h4>
      <time class="p-0 m-0 text-sm text-gray-600/80 dark:text-gray-300/80">{date}</time>
    </div>
  </div>
  <div class="relative flex flex-col gap-2 pb-4 text-gray-700 dark:text-gray-200 md:col-span-3">
    {description}
    {
      link && (
        <LinkInline href={link}>
          Saber más sobre {company}{" "}
          <ArrowRight class="size-5" aria-hidden="true" />
        </LinkInline>
      )
    }
  </div>
</div>
